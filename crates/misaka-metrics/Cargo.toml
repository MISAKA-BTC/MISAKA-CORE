[package]
name = "misaka-metrics"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "MISAKA metrics infrastructure — SLO-driven prometheus metrics"

[dependencies]
prometheus = "0.14"
once_cell = "1"
tracing = { workspace = true }
