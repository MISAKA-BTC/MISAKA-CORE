# MISAKA Network — Public Node (テストネット参加者用)
# このまま起動するだけで seed に接続して同期を開始します。

[chain]
chain_id = 2
chain_name = "MISAKA Testnet"

[node]
mode = "public"
data_dir = "./misaka-data"
log_level = "info"

[p2p]
port = 6691
max_inbound_peers = 32
max_outbound_peers = 8

[rpc]
port = 3001

[consensus]
fast_block_time_secs = 2

[faucet]
enabled = false
