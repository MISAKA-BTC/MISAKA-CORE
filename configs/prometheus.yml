global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: misaka-testnet
    metrics_path: /metrics
    static_configs:
      - targets: ['127.0.0.1:3001']
        labels:
          network: testnet
          chain_id: '2'
          node: genesis-sr0
